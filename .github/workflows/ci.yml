name: Ci

on:
  push:
    branches:
      - *

    tags:
      - v*

  pull_request:

env:
  IMAGE_NAME: omnibus-ci

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Run tests
        run: |
          make ci
